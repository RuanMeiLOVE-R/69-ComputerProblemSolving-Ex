void main() {
    IO.println("Enter num1");
    int num1 = Integer.parseInt(IO.readln());
    IO.println("Enter num2");
    int num2 = Integer.parseInt(IO.readln());
    int total = num1 + num2;

    IO.println(" คำตอบคือ " + total);
}